version: 2.1
jobs:
  build:
    docker:
    - image: cimg/node:16.16
    steps:
    - checkout
    - run:
        name: Start Pipeline
        command: |
          echo 'Starting pipeline'
#    - run:
#        name: Install dependencies
#        command: npm install

    - run:
        name: Run Tests
        command: npm run test